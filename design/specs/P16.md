# P16 · 匿名拍照试用 Guest Capture（`15_guest_capture.html`）

## 1. 状态枚举
- `quota_ok` · anon quota ≤ 3/日 · 可拍照
- `quota_warn` · 第 3 次使用 · 显示"今日还剩 1 次"
- `quota_exceeded` · 4 次 · 429 · 引导注册
- `uploading` · 同 P03 上传流
- `result_preview` · 匿名解析完成 · 显示 AI 结果 · 底部"注册保存"引导

## 2. 数据字段 → API 映射（SC-12 匿名体验）
| UI 字段 | API |
|---|---|
| 创建匿名 session | `POST /anon/session {device_fp_hash}` |
| 匿名 quota 查询 | `GET /anon/quota/{anon_id}`（Token Bucket 3/日）|
| 匿名上传 | `POST /files/presign` + `POST /anon/wrongbook {ocr_text}` |
| 匿名解析 | `GET /ai/analysis/{itemId}`（同登录流）|

## 3. testid
| 元素 | testid |
|---|---|
| quota 显示 | `guestcapture.quota.badge` |
| 拍照 CTA | `guestcapture.camera.btn` |
| 429 引导注册 | `guestcapture.quota-exceeded.register-cta` |
| 结果 → 注册 CTA | `guestcapture.result.register-cta` |

## 4. 埋点
- `guestcapture_page_view` · `{anon_id, quota_remaining}`
- `guestcapture_try_click`
- `guestcapture_quota_exceeded` · 触发 429
- `guestcapture_register_cta_click` · `{from: quota|result}`

## 5. 响应式
- 同 P03 · 全屏相机预览

## 6. 深色模式
- 同 P03
