{
  "name": "@longfeng/api-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "S7 · typed clients for S3/S4/S6 · manual skeleton（后续 openapi-typescript 生成替换）",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo '[api-contracts] source-only · consumed as TS'",
    "test": "vitest run",
    "lint": "echo '[api-contracts] lint noop'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6",
    "msw": "^2",
    "vitest": "^1"
  }
}
