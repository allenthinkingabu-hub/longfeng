# L3 hybrid 真链路 E2E 联调环境变量
# 用法：pnpm dev --mode hybrid --port 9174
#
# 关键作用：
#   1. VITE_DISABLE_MSW=1  → main.tsx 跳过 MSW worker.start() · 真请求走 vite proxy → 真 BE
#   2. VITE_API_PROXY_TARGET → vite.config.ts 把 /api/* 转发到 gateway:9080
#
# 维护人：QA Orchestrator
# 创建于：S7 微信登录 hybrid 联调 (2026-05-04)

VITE_DISABLE_MSW=1
VITE_API_PROXY_TARGET=http://localhost:9880
