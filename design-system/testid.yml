# Sd.6 · testid 规约权威 yml · 主文档预期路径
# 命名规则：<screen>.<region>.<element>[-{variant}] · 三段 kebab-case
# 与 frontend/packages/testids/src/index.ts TEST_IDS 常量 1:1 对应
# 双端同名（H5 data-testid · miniapp data-test-id）
# 见 design/system/testid-convention.md（详细规范）
generated_from: frontend/packages/testids/src/index.ts
generated_at: 2026-04-27T00:00:00+08:00
schema_version: 1.0

screens:
  capture:
    sc_owner: [SC-01.AC-1, SC-07.AC-1, SC-11.AC-1]
    testids:
      - capture.root
      - capture.form.submit
      - capture.form.subject
      - capture.form.stem
      - capture.form.draft-hint
      - capture.form.tags
      - capture.camera.btn
      - capture.gallery.btn
      - capture.manual.btn
      - capture.size-exceeded.toast
      - capture.ocr-fallback.banner
      - capture.upload-progress

  wrongbook_list:
    sc_owner: [SC-08.AC-1, SC-15.AC-1]
    testids:
      - wrongbook.list.root
      - wrongbook.list.filter-bar
      - wrongbook.list.filter-subject
      - wrongbook.list.filter-tag
      - wrongbook.list.filter-difficulty
      - wrongbook.list.item-card
      - wrongbook.list.active-tab
      - wrongbook.list.archive-tab
      - wrongbook.list.load-more
      - wrongbook.list.empty
      - wrongbook.list.skeleton

  wrongbook_detail:
    sc_owner: [SC-02.AC-1, SC-03.AC-1, SC-04.AC-1]
    testids:
      - wrongbook.detail.root
      - wrongbook.detail.stem-text
      - wrongbook.detail.image-view
      - wrongbook.detail.tag-sheet
      - wrongbook.detail.tag-chip
      - wrongbook.detail.tag-custom-input
      - wrongbook.detail.tag-save
      - wrongbook.detail.explain-stream
      - wrongbook.detail.cause-chip
      - wrongbook.detail.similar-card
      - wrongbook.detail.review-entry
      - wrongbook.detail.delete.btn
      - wrongbook.detail.delete.confirm
      - wrongbook.detail.delete.cancel

  common:
    sc_owner: [cross-screen]
    testids:
      - common.back.btn
      - common.error.banner
      - common.confirm.modal

# Phase 扩展占位（s8 复习执行 / s11 匿名 / s10 设置等后续 Phase 加入）
future_screens:
  - id: review_today        # SC-07/08/09/10 owner Phase = s5 + s8
  - id: review_exec         # SC-08 owner = s8
  - id: review_done
  - id: calendar_month      # SC-13 owner = s11/s3
  - id: event_detail
  - id: notifications
  - id: settings
  - id: landing             # SC-12/13 owner = s11
  - id: guest_capture
  - id: shared
  - id: welcomeback
  - id: observer
